project.version = '3.0.0.0'
project.ext.networkName = 'pangle'

apply from: '../shared-build.gradle'
repositories {
    flatDir {
        dirs 'libs'
    }
}
dependencies {
    compile(name: 'open_ad_sdk_3.0.0.0', ext: 'aar')
}
